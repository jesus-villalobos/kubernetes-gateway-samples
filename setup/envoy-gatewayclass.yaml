# GatewayClass backed by the Envoy Gateway controller. Samples reference this by
# name (gatewayClassName: eg).
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: eg
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
